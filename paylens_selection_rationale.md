# PayLensを選ぶべき理由

## 結論

今回のポートフォリオ案の中で、**PayLensは「ユーザー価値の分かりやすさ」「技術的な見せ場」「個人開発としての現実性」のバランスが非常に良い案**です。

特に、ポートフォリオとして評価されやすい

- 入力データを受け取る
- バックエンドで解析・加工する
- DBへ構造化して保存する
- 過去データと比較する
- グラフや集計として可視化する

という一連の処理を、ひとつのプロダクトの中で自然に見せられる点が強みです。

単なるCRUDアプリでも、単なる生成AIアプリでもなく、

> 「PDFという非構造データを、ユーザーが使える情報へ変換する」

という明確な価値があります。

そのため、**完成したときに何を作ったのかが他人へ伝わりやすく、技術面でも説明できることが多い**という意味で、PayLensは非常に有力です。

---

# 1. PayLensとは何か

PayLensは、給与明細PDFをアップロードすると、給与・控除・手取りなどの主要項目を抽出し、月ごとの推移や過去比較、年間累計、年収見込みなどを確認できる個人向けWebアプリです。

ユーザー側から見ると、

```text
毎月バラバラのPDFで受け取る給与明細
↓
アップロード
↓
必要な数字を自動でデータ化
↓
月ごとの変化を比較
↓
年間の傾向を把握
```

という流れになります。

この「PDFを見るだけ」だった情報を「蓄積して分析できるデータ」に変えることが、PayLensの中心的な価値です。

---

# 2. PayLensを選ぶべき最大の理由

## 2-1. プロダクトの価値を一言で説明しやすい

PayLensの強みは、初見の人にも価値が伝わりやすいことです。

例えば、

> 毎月給与明細は見るけれど、先月と何が変わったのか、年間でいくら稼いでいるのか、控除がどう増減したのかまでは意外と把握していない。  
> PayLensは、給与明細PDFをアップロードするだけで、それらを自動的にデータ化・比較できるようにするアプリです。

と説明できます。

ProjectVoiceやFacePrepはSES業界の業務を理解している人には非常に刺さりますが、業界外の人へ説明すると一定の前提説明が必要です。

一方で給与明細は多くの社会人にとって身近です。

**「誰の、どんな困りごとなのか」が直感的に伝わる**という点では、PayLensはかなり強いです。

---

# 3. ポートフォリオとして見栄えが良い

PayLensは、デモしたときの変化が非常に分かりやすいです。

```text
PDFをアップロード
↓
解析結果が表示される
↓
確認・修正する
↓
保存する
↓
過去月と比較される
↓
グラフに反映される
```

という一連の流れを、実際の画面操作だけで説明できます。

これはポートフォリオにおいて大きなメリットです。

例えば通常のCRUDアプリの場合、

> 登録・編集・削除できます

だけでは、技術的な工夫が伝わりづらいことがあります。

PayLensの場合は、

> 「PDFだったものがデータになった」

という目に見える変化があります。

そのため、短時間のデモでも「何を実装したのか」が伝わりやすいです。

---

# 4. CRUDだけで終わらない

PayLensでは、一般的なWebアプリの基本要素に加えて、データ処理系の開発も経験できます。

具体的には、

- Reactでの画面実装
- Python API
- DB設計
- ファイルアップロード
- PDF解析
- データ変換
- バリデーション
- 編集・修正画面
- 集計処理
- 月次比較
- グラフ表示
- 認証
- ファイル管理

などです。

特に、

> ファイルを受け取って中身を解析し、必要な情報へ変換する

という処理は、一般的なCRUDアプリではなかなか経験できません。

---

# 5. AIに頼りきりにならず、自分たちの実装を説明できる

PayLensは、必ずしもAIを使う必要がありません。

例えばテキスト埋め込み型PDFであれば、

```text
PDF
↓
テキスト抽出
↓
行・項目を解析
↓
金額を取得
↓
DBへ保存
```

という処理を、自分たちのコードで実装できます。

そのため、面接でも、

> PDF解析ライブラリを使いました

だけではなく、

> 抽出したテキストをどのように項目単位へ変換したか  
> 同じ項目でも表記が違う場合をどう扱ったか  
> 数値として妥当かどうかをどう検証したか  
> 読み取りに失敗した場合にどう修正できる設計にしたか

という説明ができます。

これはかなり重要です。

生成AIアプリの場合、

> APIへ文章を送って結果を表示した

だけに見えてしまうリスクがあります。

PayLensは、ライブラリを使ったとしても**自分たちが設計する処理が中心に残ります。**

---

# 6. 「解析に失敗する」という現実的な問題を設計できる

PayLensの良さは、PDF解析が完璧ではないこと自体が、良い設計テーマになることです。

例えば、

```text
PDF
↓
自動解析
↓
解析結果確認画面
↓
ユーザーが修正
↓
確定
↓
保存
```

というフローにできます。

つまり、

> 自動化するけれど、100%正しい前提にはしない

という設計です。

これは実際の業務システムでも非常に重要な考え方です。

「AIや自動解析を使いました」という話だけでなく、

> 自動処理が失敗することを前提として、Human-in-the-loopのUIを設計した

と説明できます。

ポートフォリオとしてかなり良い題材です。

---

# 7. データが蓄積されるほど価値が増える

PayLensは、一回使って終わるツールではありません。

毎月給与明細を追加することで、

- 月別手取り
- 基本給推移
- 残業代推移
- 控除額推移
- 年間累計
- 年収見込み
- 前年同月比較

などが可能になります。

つまり、

> 使えば使うほど自分専用の給与データベースになる

という特徴があります。

これは「継続利用する理由」が明確だということです。

---

# 8. 機能追加の方向性が自然

PayLensは、MVPを小さく作った後の拡張も自然です。

例えば、

### Phase 1

- PDFアップロード
- 主要項目抽出
- 読み取り結果修正
- 保存
- 月別一覧

### Phase 2

- 月次比較
- グラフ
- 年間累計
- 年収見込み

### Phase 3

- 複数フォーマット対応
- OCR対応
- 賞与明細
- 源泉徴収票
- 前年比較

### Phase 4

- 控除の変化検知
- 給与変化の通知
- 自然言語でのデータ質問

という形で拡張できます。

MVPと将来機能の境界が分かりやすいことも、個人開発として大きなメリットです。

---

# 9. 他の案と比較したときの強み

## vs ProjectVoice

ProjectVoiceは、業務システムとして非常に良い企画です。

特に、

- RBAC
- 社内運用
- 投稿管理
- 業務フロー

などを経験できる点ではPayLensより優れています。

一方、PayLensには、

- 誰でも理解しやすいテーマ
- ファイル解析という技術的な見せ場
- 個人でも実際に使える
- 1人のユーザーから価値が成立する

という強みがあります。

ProjectVoiceは利用者が増えて初めて価値が高まるプロダクトですが、PayLensは自分1人でも実際に使えます。

---

## vs FacePrep

FacePrepはAI活用まで含めれば技術的に非常に魅力的です。

一方で、

> 案件と社員の情報から想定質問を作る

という中心価値は、作り方によってはChatGPTのラッパーに見えてしまう可能性があります。

もちろん独自の比較ロジックを入れれば解決できますが、その設計が重要になります。

PayLensの場合は、

> PDF解析  
> → 構造化  
> → DB  
> → 比較  
> → 可視化

という自前の処理が明確なので、AIラッパー問題が起きにくいです。

---

## vs 配置最適化

配置最適化は、アルゴリズム面でPayLensより挑戦度が高いです。

特に、

- 制約充足
- 探索
- 最適化
- スコアリング

を経験できる点は魅力です。

ただし最大のリスクは、

> 本当にその課題が存在するのか

という点です。

ヒアリングで課題が確認できれば非常に強い企画ですが、確認できなければ学習用アルゴリズムになってしまう可能性があります。

PayLensは、給与明細を毎月PDFで管理している人が存在すること自体は想像しやすく、課題の説明が容易です。

---

## vs ITシステム異常検知

異常検知はPython・統計・時系列データを学ぶならPayLensより上です。

一方、プロダクトとしては既存の監視サービスが非常に強い領域です。

また、サンプルデータだけを使う場合は学習アプリ感が出やすくなります。

PayLensは、実際にユーザー自身が持っているPDFを入力データにできるため、個人向けプロダクトとして成立させやすいです。

---

## vs SideQuest

SideQuestはUIやデモとして楽しく、一般ユーザーにも伝わりやすいです。

一方、

> ChatGPTに「今日何したらいい？」と聞くのと何が違うのか

という問題があります。

差別化のためには位置情報、実在スポット、履歴、推薦ロジックが必要です。

PayLensは、ユーザー自身の給与明細という独自データを扱うため、ChatGPT単体では代替しづらいです。

---

## vs FridgeMate

FridgeMateは完成させやすさではPayLensより上です。

ただし、

- CRUD
- 賞味期限管理
- レシピ提案

という構成は比較的一般的です。

PayLensはファイル解析・構造化という一段異なる処理があるため、ポートフォリオとして技術的な差別化がしやすいです。

---

## vs フィジカルAI

フィジカルAIは技術的インパクトでは圧倒的です。

一方で、

- 画像認識モデル
- カメラ
- 推論環境
- リアルタイム処理

などの難所が多く、自分たちが本当に理解したいReact / Python / API / DBの学習から離れる可能性があります。

PayLensは難易度が一段低い分、**仕組みを理解しながら最後まで完成させられる可能性が高い**です。

---

# 10. PayLensの弱点・懸念点

PayLensにも明確な弱点があります。

これらは隠すより、最初から説明した方が説得力があります。

## 10-1. PDFフォーマット問題

最大の問題です。

給与明細は会社ごとに形式が違います。

- 項目名
- 項目位置
- 表構造
- PDF形式
- 画像PDFかテキストPDFか

が異なるため、

> あらゆる給与明細を自動解析する

という仕様は、個人開発としては現実的ではありません。

### 対応

MVPでは、

> 対応フォーマットを1〜2種類に限定する

と明言します。

これは逃げではなく、適切なスコープ設計です。

---

## 10-2. 個人情報・セキュリティ

給与明細は非常にセンシティブな情報です。

そのため、

- 認証
- アクセス制御
- ファイル保存方法
- ログへの個人情報出力
- 本番環境での扱い

を慎重に考える必要があります。

### 対応

ポートフォリオでは、

- 架空の給与明細
- ダミーデータ

を使います。

また、

> 本番運用を想定した場合は暗号化・ストレージ設計・ログ管理が必要

とREADMEに明記すれば、逆にセキュリティを意識していることを示せます。

---

## 10-3. ProjectVoiceより業務システム設計経験が少ない

ProjectVoiceでは、

- 社員
- 営業
- 管理者
- 投稿者

など複数の利用者を想定した権限設計ができます。

PayLensは基本的に個人利用なので、RBACや複雑な業務フローを経験する機会は少なくなります。

これはProjectVoiceに明確に劣る部分です。

---

## 10-4. 配置最適化よりアルゴリズム的な挑戦は少ない

PayLensにも文字列解析やデータ処理はありますが、本格的な探索アルゴリズムや最適化を扱うわけではありません。

「アルゴリズム力を見せたい」という目的なら、配置最適化の方が優れています。

---

## 10-5. フィジカルAIほど技術的インパクトはない

物体検出がリアルタイムで動くデモと比較すると、PayLensは見た目のインパクトでは劣ります。

ただし、ポートフォリオでは

> 難しい技術を使ったか

だけではなく、

> 自分で理解して設計・実装できているか

も重要です。

その意味では、PayLensの方が説明の深さを出しやすい可能性があります。

---

# 11. それでもPayLensを選ぶ理由

PayLensが最も難しい企画というわけではありません。

最も革新的な企画というわけでもありません。

最もAIらしい企画でもありません。

それでも推す理由は、

> **技術的に一段挑戦があり、それでいて自分たちで理解して完成させられる範囲にあり、完成したものの価値も他人へ説明しやすいから**

です。

難しすぎる企画では、

- ライブラリを動かすだけ
- AIに聞きながら実装するだけ
- 最後まで完成しない

というリスクがあります。

逆に簡単すぎる企画では、

- CRUDだけ
- チュートリアルと大差ない
- 技術選定や工夫を説明しづらい

という問題があります。

PayLensはその中間にあります。

```text
簡単すぎない
↑
PayLens
↓
難しすぎない
```

という位置にいること自体が、今回のポートフォリオでは強みです。

---

# 12. 他人を納得させるための説明

## 短い説明

> PayLensを選びたい理由は、ユーザー価値と技術的な挑戦のバランスが良いからです。  
> 給与明細PDFをアップロードして、内容を構造化し、DBに蓄積して、月ごとの比較やグラフとして見せるところまで一連の処理を作れます。  
> 単なるCRUDや生成AIアプリではなく、PDF解析・データ変換・バリデーション・集計・可視化まで経験できます。  
> 一方で、画像AIや高度な最適化ほど難しすぎないため、自分たちで仕組みを理解しながら完成させられる可能性も高いと考えています。

---

## もう少し詳しく説明する場合

> PayLensは、今回の候補の中で一番高度な技術を使う案ではありません。  
> ただ、ポートフォリオでは「難しい技術を使ったこと」より、「なぜその技術を使い、どういう問題を解いたかを自分で説明できること」が重要だと思っています。  
> 
> PayLensなら、給与明細PDFという非構造データを読み取り、必要な項目を構造化してDBに保存し、過去データと比較して可視化するという、一連のバックエンド処理を経験できます。  
> React・Python・API・DBという基本技術に加えて、PDF解析、データ正規化、バリデーション、ファイル管理、グラフまで扱えます。  
> 
> また、給与明細というテーマは業界知識がなくても価値が伝わりやすく、完成後のデモも分かりやすいです。  
> 
> もちろん、会社ごとにPDF形式が違うという大きな課題があります。  
> ただ、最初から全形式へ対応するのではなく、MVPでは特定フォーマットに限定して、解析結果をユーザーが確認・修正できる設計にすれば現実的です。  
> 
> 最も難しい企画ではないものの、簡単すぎず、AI任せにもなりにくく、完成させたときに技術的な工夫を説明しやすい。  
> そのバランスを考えると、PayLensが今回のポートフォリオとしてかなり良い選択だと考えています。

---

# 13. 面接・発表で特に強調したいポイント

PayLensを作る場合、以下を説明できる状態にすると非常に強いです。

### なぜ給与明細なのか

> 毎月確認する情報なのに、PDF単位で分断されていて、時系列で比較しづらいから。

### なぜPDF解析をするのか

> 手入力では利用負担が高く、継続利用されにくいため。

### なぜ完全自動化しないのか

> PDFフォーマットにはばらつきがあり、解析を100%正しい前提にするのは危険だから。  
> 自動解析後にユーザーが確認・修正する設計にした。

### なぜ対応形式を限定したのか

> MVPでは課題の中心部分を検証することを優先し、PDF解析の汎用化で開発期間を使い切らないため。

### どこを自分たちで実装したのか

> PDFから取得したテキストを、給与項目としてどのように分類・正規化するか、どの値を保存するか、入力値の妥当性をどう確認するか、月次比較をどう計算するかを設計・実装した。

---

# 14. PayLensをさらに強いポートフォリオにする条件

PayLensを選ぶなら、以下の4点は特に重要です。

## 1. PDF解析をブラックボックスにしない

ライブラリで取得したテキストをそのままAIへ渡すだけではなく、

- 項目名のマッピング
- 数値変換
- 空欄処理
- 重複処理
- バリデーション

などを自分たちで設計します。

## 2. 読み取り結果の確認画面を作る

100%自動解析より、

> 自動解析 + 人間による確認

の方が現実的で、設計としても評価されます。

## 3. 単なるPDFビューアで終わらせない

最低でも、

- 月次比較
- 年間累計
- グラフ

まで入れます。

ここまで実装して初めて、

> PDFをデータにする価値

が明確になります。

## 4. 実データを使わない

GitHubやデモには、必ず架空の給与明細を使用します。

---

# 15. 最終的な主張

PayLensを選ぶべき理由は、

**「一番難しいから」でも「一番新しいから」でもありません。**

PayLensは、

- ユーザーの困りごとが分かりやすい
- 一人でも価値が成立する
- デモで価値を伝えやすい
- CRUD以上のバックエンド処理がある
- React / Python / API / DBを一通り使える
- PDF解析という新しい挑戦がある
- AIへ処理を丸投げしなくても成立する
- MVPを現実的に絞れる
- 完成後の拡張も考えやすい

という特徴があります。

他の案には、PayLensより優れている部分があります。

- ProjectVoiceの方が業務システム設計は深い
- FacePrepの方がAI活用は自然
- 配置最適化の方がアルゴリズム的な挑戦は大きい
- 異常検知の方がPythonデータ分析を深く学べる
- フィジカルAIの方が技術的インパクトは大きい

それでもPayLensを推すのは、

> **「ユーザーにとって意味のあるプロダクトを、技術的な挑戦を含めつつ、自分たちで理解して完成させる」**

という今回のポートフォリオの目的に対して、最もバランスが良い選択肢の一つだからです。

もし「完成度の高い1本を作り、それを自分の言葉で深く説明できること」を重視するなら、PayLensを選ぶ合理性は十分にあります。
